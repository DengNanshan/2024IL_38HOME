1 构建模仿学习IL 模型
2 设计状态空间
3构建三种环境动力学
4设计状态空间
5构建配置文件
6 生成训练数据
7 调整超参数
8 修改数据的动作空间，调整顺序

9 重新训练退火策略，跑验证集
10 退火+ 调整空间
11. 增加check point
12 绘制不同的测试集的曲线
13 写测试多个模型在多个环境下表现的代码  /开环紧以碰撞率做测试 test_muti_model_env.py 2024年7月9日18:00:45
14 v2版本的代码调整了模型的大小，训练输入输出映射，学习率为指数
15 v3代码修改为指余弦退火, 结果不好，使用300轮次退火发现在100论的10e-2已经是极限了。 新v3 大幅增加参数量
16  因为初始得到时候缺少数据，一开始就碰撞，想办法修改初始车辆   修改后使用环境名称为
 “env = gym.make('highway-random-pose-v0', render_mode="rgb_array")” 但仍然碰撞 ，最终实验考虑增加训练数据


todo
1 做测试 done
2 增加 a)地理特异性 b)全局位置

results
1. 单独训练后，随训练时间增加，碰撞率（开环），测试集的偏差变化（闭环）
2. 地理特异性与全局位置和单独位置的对比。
    预期结果： 在算力不变的情况下，使用的存储空间越多越精确，训练效果越好。 全局收敛极其慢且在未知区域表现不好。（没有位置区域）
    a) 相同时间算力下的整体碰撞率表现和相似度表现。 b) 特定地点的表现
